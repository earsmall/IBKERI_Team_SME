# 읽을거리 업데이트 작업 지침

이 파일은 `index.html`에 읽을거리 항목을 안전하게 추가하기 위한 작업 지침과 신규 데이터 원본이다.

## 1. 작업 목적

- `index.html`의 기존 자료는 절대 수정하거나 삭제하지 않는다.
- 이번 파일에 적힌 **새 날짜 데이터만** `index.html`에 추가한다.
- 이미 `index.html`에 존재하는 날짜라면, 자동으로 덮어쓰지 말고 작업을 멈춘 뒤 사용자에게 확인한다.

## 2. 절대 규칙

- `index.html`의 과거 날짜 데이터는 어떤 경우에도 삭제, 수정, 덮어쓰기하지 말고 반드시 그대로 둔다.
- 기존 날짜 섹션의 제목, 본문, 링크, 순서, 스타일을 수정하지 않는다.
- 새 날짜 1개를 추가할 때는 그 날짜 섹션만 새로 만든다.
- 같은 날짜를 2번 추가하지 않는다.
- 정보가 없는 항목은 추측해서 채우지 않는다.
- `주요 내용`이 `없음`이면 `index.html`에는 표시하지 않는다.
- `주요 내용`이 있으면 개수와 관계없이 모든 줄을 반드시 `- `로 시작하는 목록으로 적는다.
- `주요 내용`의 `- `는 단순 입력 형식이 아니라 웹페이지에 실제로 보여야 하는 표시이므로 제거하거나 숨기지 않는다.
- `첨부`가 `없음`이면 `index.html`에는 표시하지 않는다.
- `첨부`가 있으면 개수와 관계없이 모든 줄을 반드시 `* `로 시작하는 목록으로 적는다.
- 첨부 링크는 반드시 클릭 가능한 링크로 넣는다.
- 첨부 텍스트는 읽을거리 본문과 구분되도록 기존 첨부 스타일(붉은 계통 링크 스타일)을 유지한다.
- 첨부 앞의 `* `도 첨부 링크와 같은 붉은색으로 웹페이지에 실제 표시되어야 한다.
- 기관명은 아래 표준 표기를 따른다.

## 3. 기관명 표준 표기

- `대외경제정책연구원`
- `국가데이터처`
- `하나금융경영연구소`
- `한국금융연구원`
- `한국무역협회`
- `한국개발연구원`
- `언론기사`
- `중기중앙회`
- `재정경제부`

### 치환 규칙

- `KIEP`, `대외경제정책연구원` -> `대외경제정책연구원`
- `통계청`, `국가데이터처` -> `국가데이터처`
- `하나금융연구소`, `하나금융경영연구소` -> `하나금융경영연구소`
- `한국금융연구원]`, `한국금융연구원` -> `한국금융연구원`
- `KITA`, `한국무역협회` -> `한국무역협회`
- `KDI`, `한국개발연구원` -> `한국개발연구원`
- `뉴스 기사` -> `언론기사`
- `중소기업중앙회`, `중기중앙회` -> `중기중앙회`
- `기획재정부`, `재정경제부` -> `재정경제부`

## 4. `index.html` 반영 규칙

- 새 날짜 섹션은 날짜 기준으로만 추가한다.
- 같은 날짜가 없을 때만 새 날짜 섹션을 생성한다.
- 날짜 섹션 안에는 아래 항목 순서대로 넣는다.
- 순서: `기관` -> `제목` -> `주요 내용` -> `첨부` -> `링크`
- `제목`은 원문 그대로 사용한다.
- `주요 내용`이 여러 개이면 각 줄을 `- `로 시작하는 목록으로 넣는다.
- `주요 내용`이 한 줄이어도 `- `로 시작하는 목록 1개로 넣는다.
- 웹페이지에도 해당 날짜의 `주요 내용`은 입력값 그대로 `- ` 목록 형식으로 반영한다.
- 웹페이지 카드에서도 `주요 내용` 각 줄 앞의 `- `가 그대로 보여야 한다.
- `첨부`가 있으면 각 줄을 `* `로 시작하는 목록으로 넣는다.
- 웹페이지 카드에서도 `첨부` 각 줄 앞의 `* `가 그대로 보여야 한다.
- 웹페이지 카드에서도 `첨부` 각 줄 앞의 `* `는 첨부 링크와 같은 붉은색으로 보여야 한다.
- `링크`는 제목 클릭 시 이동하는 대표 링크로 사용한다.
- `첨부`는 본문 아래 별도 링크로 넣는다.

## 5. 입력 형식 규칙

- 날짜 헤더는 반드시 `# 날짜: YYYY년 M월 D일` 형식으로만 적는다.
- 각 읽을거리 항목은 반드시 `---`로 구분한다.
- 각 항목은 아래 5개 필드를 모두 적는다.
- 필드 이름은 반드시 정확히 아래와 같이 쓴다.
  `- 기관:`
  `- 제목:`
  `- 주요 내용:`
  `- 첨부:`
  `- 링크:`
- 값이 없으면 빈칸으로 두지 말고 반드시 `없음`이라고 적는다.
- `주요 내용`에 값이 있으면 `- 주요 내용:` 아래 줄부터 각 항목을 반드시 `  - ` 형식으로 적는다.
- `첨부`에 값이 있으면 `- 첨부:` 뒤에 적는 첨부파일명 목록을 웹페이지에서 `* ` 목록으로 표시한다.
- 이때 첨부 줄의 `* `와 첨부 링크 텍스트는 둘 다 같은 붉은색 첨부 스타일로 표시한다.
- `첨부`는 아래 형식만 허용한다.
  `첨부파일명(https://...)`
- 첨부가 여러 개면 ` | `로 구분한다.
- 날짜 하나당 읽을거리 본문은 한 번만 작성한다.

## 6. 작업 순서

1. `index.html`에 같은 날짜가 이미 있는지 먼저 확인한다.
2. 같은 날짜가 없으면 새 날짜 섹션만 추가한다.
3. 기존 날짜 내용은 읽기만 하고 수정하지 않는다.
4. `주요 내용: 없음`은 출력하지 않는다.
5. `첨부: 없음`은 출력하지 않는다.
6. 제목 링크와 첨부 링크가 올바른지 확인한다.
7. 기관명이 표준 표기와 다르면 표준 표기로 바꾼다.
8. 웹페이지에서 `주요 내용`이 보일 때 각 줄 앞의 `- `가 실제로 노출되는지 확인한다.
9. 웹페이지에서 `첨부`가 보일 때 각 줄 앞의 `* `가 실제로 노출되는지 확인한다.
10. 웹페이지에서 첨부 줄의 `* `와 첨부 링크 텍스트가 둘 다 같은 붉은색인지 확인한다.

## 7. 신규 데이터 입력 템플릿

아래 템플릿만 복사해서 새 날짜를 추가한다.

```md
# 날짜: YYYY년 M월 D일
## 읽을거리 내용

---

- 기관: 기관명
- 제목: 제목
- 주요 내용: 없음
- 첨부: 없음
- 링크: https://...

---

- 기관: 기관명
- 제목: 제목
- 주요 내용:
  - 첫 번째 핵심 내용
  - 두 번째 핵심 내용
- 첨부: * 첨부파일명1(https://...) | * 첨부파일명2(https://...)
- 링크: https://...
```

## 8. 이번 반영 데이터

# 날짜: 2026년 5월 15일
## 읽을거리 내용

---

- 기관: 지식재산처
- 제목: 경제 불확실성 걷혔다...’25년 산업재산권 출원 ‘쑥’
- 주요 내용: 없음
- 첨부: 없음
- 링크: https://eiec.kdi.re.kr/policy/materialView.do?num=281087&pg=1&pp=100&device=pc&search_txt=&topic=&type=A&depth1=&depth2=